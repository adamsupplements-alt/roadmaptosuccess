# LQR Road Map to SUCCESS (Streamlit)

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Vault files
Put your PDFs/DOCX inside `vault/` to match the existing relative links used in the HTML.
